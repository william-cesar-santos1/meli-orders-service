apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    editable: false
    jsonData:
      # Aumenta a janela máxima de query para facilitar exploração local.
      maxLines: 1000

