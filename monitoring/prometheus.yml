global:
  scrape_interval: 5s

scrape_configs:
  - job_name: meli-orders
    static_configs:
      - targets: ['host.docker.internal:8080']
    metrics_path: /actuator/prometheus

rule_files:
  - alerts.yml

