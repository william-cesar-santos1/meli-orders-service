# SOLUCAO: MongoDB removido. H2 removido. O datasource e o catlogo sao injetados via @DynamicPropertySource.
# Nenhuma infraestrutura e configurada estaticamente — cada teste controla a sua.
spring:
  jpa:
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: localhost
      port: 6379
  elasticsearch:
    uris: http://localhost:9200
